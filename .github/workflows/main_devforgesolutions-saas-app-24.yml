name: Deploy KinderHub to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: kinderhub-production-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci --omit=dev

      - name: Smoke test application startup
        run: node -e "const { app } = require('./src/app'); if (!app) process.exit(1); console.log('Express app loaded')"

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: 05bdafa0-0d05-4734-90f8-1cd13ff91410
          tenant-id: bb2c8b70-1ec5-40ea-b52a-263706eab1fa
          subscription-id: 5a7ad0a9-17e6-45e4-987b-3db59aca4abc

      - name: Configure Azure package deployment
        run: |
          az webapp config appsettings set \
            --name kinderhub-saas-app \
            --resource-group rg-kinderhub-prod \
            --settings SCM_DO_BUILD_DURING_DEPLOYMENT=false ENABLE_ORYX_BUILD=false

      - name: Create deployment package
        run: |
          rm -f release.zip
          zip -r release.zip . \
            -x "release.zip" \
            -x ".git/*" \
            -x ".github/*" \
            -x ".env" \
            -x ".azure-*.local.json" \
            -x "*.log" \
            -x "azure-logs/*" \
            -x "backup/*" \
            -x "test-results/*" \
            -x ".venv/*" \
            -x ".vscode/*"

      - name: Publish package and restart Azure Web App
        run: |
          storage_account="kinderhubstza115707"
          container="app-packages"
          blob_name="release-${GITHUB_SHA}.zip"
          expiry=$(date -u -d "30 days" '+%Y-%m-%dT%H:%MZ')
          storage_key=$(az storage account keys list \
            --account-name "$storage_account" \
            --resource-group rg-kinderhub-prod \
            --query "[0].value" \
            -o tsv)

          az storage container create \
            --account-name "$storage_account" \
            --account-key "$storage_key" \
            --name "$container" \
            --public-access off \
            --output none

          az storage blob upload \
            --account-name "$storage_account" \
            --account-key "$storage_key" \
            --container-name "$container" \
            --name "$blob_name" \
            --file release.zip \
            --overwrite true \
            --output none

          sas=$(az storage blob generate-sas \
            --account-name "$storage_account" \
            --account-key "$storage_key" \
            --container-name "$container" \
            --name "$blob_name" \
            --permissions r \
            --expiry "$expiry" \
            --https-only \
            -o tsv)
          package_url="https://${storage_account}.blob.core.windows.net/${container}/${blob_name}?${sas}"

          az webapp config appsettings set \
            --name kinderhub-saas-app \
            --resource-group rg-kinderhub-prod \
            --settings WEBSITE_RUN_FROM_PACKAGE="$package_url" SCM_DO_BUILD_DURING_DEPLOYMENT=false ENABLE_ORYX_BUILD=false \
            --output none

          az webapp restart \
            --name kinderhub-saas-app \
            --resource-group rg-kinderhub-prod \
            --output none

      - name: Verify Azure health
        run: |
          for attempt in {1..30}; do
            status=$(curl -sS -o /tmp/health.json -w "%{http_code}" https://kinderhub-saas-app.azurewebsites.net/health || true)
            if [ "$status" = "200" ]; then
              cat /tmp/health.json
              exit 0
            fi

            echo "Health check attempt ${attempt} returned ${status}; waiting..."
            sleep 10
          done

          cat /tmp/health.json || true
          exit 1
